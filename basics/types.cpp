#include <iostream>

int main(){
    // INTEGER
    int integr = 5;
    std::cout << integr << std::endl;

    // DOUBLE - represents a fractional value
    double db = 2.9;
    std::cout << db << std::endl;

    // FLOAT - also represents a fractional value
    float flt = 6.9;
    std::cout << flt << std::endl;

    // CHARACTER (char)
    // char word = "string"; // chars are a single character enclosed in single quotes
    char letter = 'A';
    std::cout << letter << std::endl;

    // BOOL

    // VOID

    // AUTO
}